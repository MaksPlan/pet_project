import { configureStore } from '@reduxjs/toolkit';

const initialState = {};

const reducer = () => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};

export const store = configureStore({ reducer: reducer });
